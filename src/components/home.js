import React from 'react';
import {MapContainer} from './map/map-container';

export const Home = () => {
    return (
        <div style={{position: "relative"}}>
            
            <MapContainer />
        </div>
    );
}